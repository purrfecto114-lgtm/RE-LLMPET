{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Octopus desktop windows may invoke the explicitly registered Rust commands and subscribe to app events.",
  "windows": ["pet", "panel"],
  "permissions": ["core:default"]
}

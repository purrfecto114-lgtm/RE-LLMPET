[package]
name = "llmpet-tauri-gate"
version = "0.1.0"
edition = "2021"

[dependencies]
tao = "0.30"
wry = "0.46"
tray-icon = "0.19"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = ["Win32_UI_WindowsAndMessaging", "Win32_Foundation"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
